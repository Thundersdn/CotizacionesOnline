<%= form_for(@usuario) do |f| %>
  <% if @usuario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usuario.errors.count, "errores") %> prohibieron la creación de este usuario:</h2>

      <ul>
      <% @usuario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rut %><br>
    <%= f.text_field :rut %>
  </div>
    <div class="field">
      <%= f.label :nombre %><br>
      <%= f.text_field :nombre %>
    </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :privilegios %><br>
    <%= f.text_field :privilegios %>
  </div>
  <div class="actions">
    <% if flash[:edit] %>
         <%= f.submit "Guardar Usuario" %>
    <%else%>
        <%= f.submit "Crear Usuario" %>
    <%end %>
  </div>
<% end %>
